import type { NextPage } from 'next';
import styles from '../styles/Home.module.scss';

const Home: NextPage = () => {
  return (
    <main className={styles.main}>
      <div>Please wait...</div>
    </main>
  );
};

export default Home;
